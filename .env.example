# Supabase
SUPABASE_URL=https://mjocggnioqptesmjotpx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=        # Dashboard → Settings → API → service_role key
SUPABASE_JWT_SECRET=              # Dashboard → Settings → API → JWT Secret

# Webhook security (genera un string aleatorio seguro, ej: openssl rand -hex 32)
WEBHOOK_SECRET=
