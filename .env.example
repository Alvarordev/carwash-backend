# Supabase
SUPABASE_URL=https://mjocggnioqptesmjotpx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=        # Dashboard → Settings → API → service_role (Secret API key)

# Webhook security (genera un string aleatorio: openssl rand -hex 32)
# Este mismo valor lo configuras en el header del webhook en Supabase Dashboard
WEBHOOK_SECRET=
